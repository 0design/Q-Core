Synthetic test-owner task, not Oleg acceptance. This is an existing project with tracked modifications, a staged new file and untracked notes; preserve all of them. Use the SDD caller workflow to generate the specification through Core inference jobs, without importing a prewritten specification. Fix only normalizeTags(input) in tags.mjs: trim/lowercase string entries including Ukrainian letters, drop empty and nonstring entries, deduplicate preserving first occurrence order, do not mutate input. Preserve the existing projectLabel export and user comment in tags.mjs. Only tags.mjs is authorized for Core file changes. README.md, settings.json, private-notes.txt, staged-plan.txt, Git history/index and verify-tags.mjs must remain unchanged. The existing verify-tags.mjs is the independent checker; use Node to run it and do not modify it. The authenticated current Codex caller may generate the spec and file proposals. Install the pinned engine separately inside this project if needed. Ask for exact spec approval before applying changes. No other providers, public sends, external writes or sibling source reads. After verification, repeat the same run without another code change and report preservation evidence.
