Additional explicit synthetic regression scenario, separate from the successful golden-path task: preserve the completed tags.mjs, original verifier and original run. In a separate local regression subdirectory, reuse the same approved functional requirements and a byte-identical copy of verify-tags.mjs, with only its local tags.mjs in writable scope. Create a new Core caller run. Ask for the exact new approval hash before execution. After that approval, deliberately submit a first files proposal whose normalizeTags returns an empty array, to exercise a real verifier failure; then answer the Core repair job in your current session with the correct implementation and require the unchanged verifier to pass. This deliberate faulty first proposal is an openly controlled test injection, not an accidental model failure. Do not edit files directly or invent verifier evidence. Keep every Core response and proposal in this separate regression directory.
